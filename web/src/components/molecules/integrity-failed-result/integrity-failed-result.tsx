import {
  AlertTriangle,
  RefreshCcw,
  X,
} from 'lucide-react'
import { Button, Text } from '@/components/atoms'
import { VerificationCheck } from '../verification-check'
import type { IntegrityFailedResultProps } from './types'

export function IntegrityFailedResult({
  onVerifyAnotherDocument,
  onContactSupport,
}: Readonly<IntegrityFailedResultProps>) {
  return (
    <div className="flex w-full max-w-xl flex-col">
      <div className="rounded-2xl bg-danger-red/10 p-4">
        <div className="flex items-start gap-3">
          <div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-danger-red text-clean-white">
            <X className="h-5 w-5" strokeWidth={3} />
          </div>
          <div>
            <Text
              as="h2"
              variant="h4"
              className="text-danger-red"
            >
              Document Integrity Failed
            </Text>
            <Text variant="caption" className="mt-1">
              The uploaded PDF does not match the original document
              generated by FlashID.
            </Text>
          </div>
        </div>
      </div>
      <div className="mt-5 space-y-4 rounded-2xl border border-border-grey bg-clean-white p-5">
        <VerificationCheck
          title="Certification record"
          status="Found"
          description="A valid certified record exists"
        />
        <VerificationCheck
          title="Document integrity"
          status="Failed"
          description="The PDF has been modified or corrupted"
          failed
        />
        <VerificationCheck
          title="Source credential"
          status="Active"
          description="The credential is still valid"
        />
      </div>
      <div className="mt-5 flex items-start gap-3 rounded-xl border border-accent-gold/40 bg-accent-gold/10 p-4">
        <AlertTriangle className="mt-0.5 h-5 w-5 shrink-0 text-accent-gold" />
        <Text
          variant="caption"
          className="text-deep-green"
        >
          The document may have been altered after it was issued,
          re-saved, or is not a certified copy from FlashID.
        </Text>
      </div>
      <div className="mt-5 flex flex-col gap-3">
        <Button
          type="button"
          variant="primary"
          LeftIcon={RefreshCcw}
          onClick={onVerifyAnotherDocument}
          className="!h-11 !w-full rounded-xl px-5"
        >
          Try Another Document
        </Button>
      </div>
    </div>
  )
}