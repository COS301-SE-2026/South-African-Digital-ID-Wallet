pre-commit:
  parallel: false
  commands:
    web-lint:
      root: "web"
      run: pnpm lint
    web-format:
      root: "web"
      run: pnpm prettier --check .
    web-build:
      root: "web"
      run: pnpm build